export class AtletaOuro {
  id_atleta_ouro: number;
  idade_categoria: number;
  peso_ideal: number;        // em kg
  altura_ideal: number;      // em metros
  tiro_ideal: number;        // percentual 0-100
  assistencia_ideal: number; // percentual 0-100
  livre_ideal: number;       // percentual 0-100
  tres_ideal: number;        // percentual 0-100
}
